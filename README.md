# The template for a new project Solana DApp

```bash
npx create-next-app@latest my-project --typescript --eslint
```

### Install tailwindcss

```bash
pnpm i tailwindcss postcss autoprefixer
npx tailwindcss init -p
```
